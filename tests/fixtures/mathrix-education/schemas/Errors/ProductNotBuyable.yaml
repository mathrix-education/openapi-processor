$extends:
  Base.yaml:
description: The request product is not buyable (its price is `null`).
required: [data]
properties:
  error:
    enum: [product_not_buyable]
  message:
    example: Product id `27` cannot be purchased
  data:
    type: object
    required: [product_id]
    properties:
      product_id:
        description: The product id.
        type: integer
        example: 27
