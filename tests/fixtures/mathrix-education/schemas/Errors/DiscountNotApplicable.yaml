$extends:
  Base.yaml:
description: The discount is not applicable.
required: [data]
properties:
  error:
    enum: [discount_not_applicable]
  message:
    example: Discount `MODEMACHINE2018` is not applicable on those products.
  data:
    type: object
    required: [discount]
    properties:
      discount:
        description: The discount code.
        type: string
        example: MODEMACHINE2018
