$extends:
  Base.yaml:
description: File not found.
required: [data]
properties:
  error:
    enum: [file_not_found]
  message:
    example: Unable to find file `test.jpg`.
  data:
    type: object
    required: [file]
    properties:
      file:
        description: The file requested.
        type: string
        example: test.jpg
