$extends:
  Base.yaml:
description: The discount has expired.
required: [data]
properties:
  error:
    enum: [discount_expired]
  message:
    example: Discount `MODEMACHINE2018` has expired.
  data:
    type: object
    required: [discount]
    properties:
      discount:
        description: The expired discount code.
        type: string
        example: MODEMACHINE2018
