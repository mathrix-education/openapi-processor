type: object
required: [page, per_page, total, data]
properties:
  page:
    description: The current page.
    type: integer
    example: 4
    readOnly: true
  per_page:
    description: The number of models per page.
    type: integer
    example: 100
    readOnly: true
  total:
    description: The total number of models processed.
    type: integer
    example: 1845
    readOnly: true
  data:
    description: The models as an array.
    type: array
    items:
      type: object
    readOnly: true
