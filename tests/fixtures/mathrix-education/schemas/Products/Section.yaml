$extends:
  Base.yaml:
properties:
  type:
    enum: [section]
  children:
    description: The training child lessons.
    type: array
    items:
      allOf:
        - $ref: '../../index.yaml#/components/schemas/ProductLesson'
