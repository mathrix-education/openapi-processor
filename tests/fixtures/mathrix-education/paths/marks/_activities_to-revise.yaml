get:
  summary: Paginate to-revise activities
  responses:
    200:
      description: The `to-revised` activity marks.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../index.yaml#/components/schemas/Mark'
