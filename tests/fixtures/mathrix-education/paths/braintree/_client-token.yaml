get:
  summary: Get the generated Client Token for Braintree
  security:
    - bearer: [logged]
  responses:
    200:
      description: The Braintree Client Token
      content:
        application/json:
          schema:
            type: object
            properties:
              token:
                description: The Braintree Client Token
                type: string
