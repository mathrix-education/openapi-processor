$extends:
  ../../fragments/paths/get_{modelId}.yaml:
    model: category
  ../../fragments/paths/patch_{modelId}.yaml:
    model: category
  ../../fragments/paths/delete_{modelId}.yaml:
    model: category
get:
  security:
    - bearer: [categories:read]
patch:
  security:
    - bearer: [categories:write]
delete:
  security:
    - bearer: [categories:delete]
