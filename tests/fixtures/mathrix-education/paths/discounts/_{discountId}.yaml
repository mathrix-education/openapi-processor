$extends:
  ../../fragments/paths/get_{modelId}.yaml:
    model: discount
  ../../fragments/paths/patch_{modelId}.yaml:
    model: discount
  ../../fragments/paths/delete_{modelId}.yaml:
    model: discount
get:
  security:
    - bearer: [discounts:read]
patch:
  security:
    - bearer: [discounts:write]
delete:
  security:
    - bearer: [discounts:delete]
