$extends:
  ../../fragments/paths/get_{page}_{perPage}.yaml:
    model: notification
get:
  summary: All notifications
  description: Retrieve all notifications by descending creation date, paginated.
  security:
    - bearer: [logged]
