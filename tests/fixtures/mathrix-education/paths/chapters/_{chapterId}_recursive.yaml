$extends:
  ../../fragments/paths/get_{modelId}.yaml:
    model: chapter
get:
  summary: Get a chapter (recursive)
  description: Get the notions, activities and modules of a chapter.
  responses:
    200:
      description: The requested notions, activities and modules.
      content:
        application/json:
          schema:
            # Todo here
