$extends:
  ../../fragments/paths/get_{modelId}.yaml:
    model: product
  ../../fragments/paths/patch_{modelId}.yaml:
    model: product
  ../../fragments/paths/delete_{modelId}.yaml:
    model: product
get:
  security:
    - bearer: [products:read]
patch:
  security:
    - bearer: [products:write]
delete:
  security:
    - bearer: [products:delete]
