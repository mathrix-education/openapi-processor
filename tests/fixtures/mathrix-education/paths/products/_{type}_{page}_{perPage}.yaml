$extends:
  ../../fragments/paths/get_{page}_{perPage}.yaml:
    model: product
get:
  summary: Paginate products by type
  parameters:
    - name: type
      in: path
      description: The product filter.
      required: true
      schema:
        type: string
        enum: [all, pack, training, section, lesson]
