get:
  summary: Index settings
  responses:
    200:
      description: The settings.
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: '../../index.yaml#/components/schemas/Setting'
