$extends:
  ../../partials/paths/get_{modelId}_{related}.yaml:
    model: series
    related: question
  ../../partials/paths/patch_{modelId}_{related}.yaml:
    model: series
    related: question
get:
  security:
    - bearer: [series:read]
patch:
  security:
    - bearer: [series:write]
