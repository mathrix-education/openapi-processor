$extends:
  ../../partials/paths/get_{modelId}.yaml:
    model: theme
  ../../partials/paths/patch_{modelId}.yaml:
    model: theme
  ../../partials/paths/delete_{modelId}.yaml:
    model: theme
get:
  security:
    - bearer: [themes:read]
patch:
  security:
    - bearer: [themes:write]
delete:
  security:
    - bearer: [themes:delete]
