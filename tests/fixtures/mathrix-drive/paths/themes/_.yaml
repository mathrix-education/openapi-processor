$extends:
  ../../partials/paths/index.yaml:
    model: theme
  ../../partials/paths/post.yaml:
    model: theme
get:
  security:
    - bearer: [themes:read]
post:
  security:
    - bearer: [themes:write]
