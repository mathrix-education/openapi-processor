description: Model not found.
content:
  application/json:
    schema:
      type: object
      properties:
        error:
          description: The error message.
          type: string
          example: model_not_found
